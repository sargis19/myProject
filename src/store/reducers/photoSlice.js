const initialState = {
    data:[],
    status: false
}

function photoSlice( state = initialState, action ) {
    switch (action.type) {    
        default: return state
    }
}

export default photoSlice