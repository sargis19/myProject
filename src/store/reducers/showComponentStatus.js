const initialState = {
    componentStatus: "photos"
}

function showComponentStatus(state = initialState, action) {
    switch (action.type) {
        default: return state
    }

}

export default showComponentStatus